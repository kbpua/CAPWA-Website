version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: capwa-website
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Uncomment if you need to mount volumes for development
    # volumes:
    #   - ./src:/app/src:ro

